{
  "name": "cărtz client",
  "version": "1.0.0",
  "description": "joc de cărtz",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "babel": "babel src --out-dir lib",
    "watch": "babel src --watch --out-dir lib",
    "pack": "browserify lib/app.js -o bin/app.min.js",
    "watchify": "watchify lib/app.js -o bin/app.min.js"
  },
  "author": "Lucian Parfeni",
  "license": "MIT",
  "dependencies": {
    "pixi.js": "^3.0.7"
  }
}
